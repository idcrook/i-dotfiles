install rustlang
=====================


```
cd ~/.dotfiles
stow -v rustlang
```


Ubuntu
------

macOS
-----

```
# show already be done by homebrew
brew install rustup-init

# accept defaults, except for dotfile edits
rustup-init
```
